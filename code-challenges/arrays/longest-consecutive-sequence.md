# Longest Consecutive Sequence

- Use a set
- Check if num is start of sequence, check if set contains num - 1
  - Check if it has a "left" neighbor, eg num - 1 exists
- If it has no left neighbor / is the start of a sequence, check if consecutive numbers exist in set

